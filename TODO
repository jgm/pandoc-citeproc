TODO:

- add some tests to this module (tests previously in pandoc?)
  (see tests directory)
- allow YAML version of csl-abbrevs.  document here and in pandoc README.
- improve "not found" behavior in citeproc-hs:
  - use nicer "???" for citations
  - or change types so warnings can be omitted
- add something about this module to pandoc's INSTALL instructions
